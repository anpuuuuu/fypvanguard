# Android 模拟器相机配置指南

## ⚠️ 问题说明

在 Windows 上使用 Android Studio 模拟器时，**默认情况下无法使用真实相机**。这是因为：
- 模拟器是虚拟环境，没有直接访问物理硬件的权限
- Windows 系统限制模拟器直接访问摄像头

## ✅ 解决方案

### 方案 1: 配置模拟器使用电脑摄像头（推荐）

#### 步骤 1: 启动模拟器时配置
1. 打开 Android Studio
2. 打开 **AVD Manager** (Tools → Device Manager)
3. 点击你的模拟器旁边的 **▼** (下拉箭头) → **Cold Boot Now**
4. 或者编辑模拟器配置：
   - 点击 **Edit** (铅笔图标)
   - 在 **Advanced Settings** 中
   - 找到 **Camera** 部分
   - 设置：
     - **Front Camera**: `Webcam0` (使用电脑前置摄像头) 或 `VirtualScene` (虚拟场景)
     - **Back Camera**: `Webcam0` (使用电脑后置摄像头) 或 `VirtualScene` (虚拟场景)

#### 步骤 2: 使用命令行启动（更可靠）
```bash
# 在 Android SDK 的 emulator 目录下运行
emulator -avd <你的AVD名称> -camera-back webcam0 -camera-front webcam0
```

#### 步骤 3: 检查摄像头是否可用
在模拟器中：
1. 打开 **Camera** 应用
2. 如果能看到画面，说明配置成功
3. 如果显示黑屏或错误，继续下一步

### 方案 2: 使用虚拟场景（VirtualScene）

如果电脑摄像头不可用，可以使用模拟器的虚拟场景：

1. 编辑 AVD 配置
2. 设置：
   - **Front Camera**: `VirtualScene`
   - **Back Camera**: `VirtualScene`
3. 这会显示一个虚拟的 3D 场景，可以用来测试相机功能

### 方案 3: 使用真实 Android 设备（最推荐）

**最佳测试方案**：使用真实的 Android 手机通过 USB 连接：

1. 在手机上启用 **开发者选项**：
   - 设置 → 关于手机 → 连续点击"版本号" 7 次
2. 启用 **USB 调试**：
   - 设置 → 开发者选项 → USB 调试
3. 连接手机到电脑
4. 在 Android Studio 中选择你的手机设备
5. 运行应用：`flutter run`

### 方案 4: 使用 Android 模拟器的扩展功能

某些模拟器（如 Genymotion）支持更好的相机集成，但需要额外配置。

---

## 🔧 快速测试方法

### 方法 1: 检查模拟器相机支持
在模拟器中运行以下命令（通过 ADB）：
```bash
adb shell dumpsys media.camera
```

### 方法 2: 测试相机应用
在模拟器中打开系统自带的 **Camera** 应用，看是否能正常使用。

### 方法 3: 使用测试应用
安装一个简单的相机测试应用，如 "Camera Test" 来验证。

---

## 📱 针对你的 QR 扫描功能

### 如果模拟器相机不可用，你可以：

1. **使用真实设备测试**（最推荐）
   - 连接 Android 手机
   - 运行 `flutter run`
   - 测试 QR 扫描功能

2. **使用模拟器的虚拟场景**
   - 配置 VirtualScene
   - 虽然不能扫描真实 QR 码，但可以测试相机界面和权限处理

3. **手动测试 QR 码验证逻辑**
   - 暂时修改代码，允许手动输入 QR 码字符串
   - 测试验证逻辑是否正确

---

## 🐛 常见问题

### Q: 模拟器显示"无法连接到相机"
**A**: 
- 检查模拟器配置中的相机设置
- 尝试使用 VirtualScene 而不是 Webcam0
- 重启模拟器

### Q: 相机权限请求后仍然无法使用
**A**: 
- 在模拟器设置中检查应用权限
- 设置 → 应用 → 你的应用 → 权限 → 相机 → 允许

### Q: 如何知道模拟器是否支持相机？
**A**: 
- 打开模拟器的 Camera 应用
- 如果能看到画面（即使是虚拟场景），说明支持
- 如果显示错误，可能需要使用真实设备

---

## ✅ 推荐测试流程

1. **开发阶段**：使用模拟器测试 UI 和基本功能
2. **相机功能测试**：使用真实 Android 设备
3. **最终测试**：在多个真实设备上测试

---

## 💡 提示

对于 QR 码扫描这种需要真实硬件的功能，**强烈建议使用真实设备进行测试**。模拟器主要用于：
- UI 开发
- 逻辑测试
- 权限流程测试（可以使用模拟）

但相机、传感器等硬件功能，最好在真实设备上测试。
